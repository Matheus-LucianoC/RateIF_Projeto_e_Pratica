package com.rateif.rateif.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.rateif.rateif.model.LogDownloadRelatorio;

public interface LogDownloadRelatorioRepository extends JpaRepository<LogDownloadRelatorio, Integer> {
}